import 'package:flutter/material.dart';

class ReytingPage
    extends StatelessWidget {
  const ReytingPage
      ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ReytingPage"
          ""),),
    );
  }
}
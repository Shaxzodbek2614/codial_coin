import 'package:flutter/material.dart';

class KitoblarPage extends StatelessWidget {
  const KitoblarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("KitoblarPage"),),
    );
  }
}
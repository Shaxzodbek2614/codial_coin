import 'package:flutter/material.dart';

class AuksionPage extends StatelessWidget {
  const AuksionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AuksionPage"),),
    );
  }
}